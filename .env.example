# OpenWeather API Configuration
OPENWEATHER_API_KEY=your_api_key_here

# Weather Data Database Configuration
# (Airflow metadata uses a separate database: airflow:airflow@postgres/airflow)
WEATHER_DB_NAME=weather_data
WEATHER_DB_USER=weather_user
WEATHER_DB_PASSWORD=change_this_password

# Airflow Configuration
# For Linux users: run `echo $(id -u)` and set AIRFLOW_UID to the result
# For macOS/Windows: you can leave this as default (50000)
AIRFLOW_UID=50000

# Airflow Admin User Credentials
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# Optional: Airflow Image Configuration
# Uncomment to customize the Airflow image
# AIRFLOW_IMAGE_NAME=apache/airflow:3.1.3-python3.13

# Optional: Project directory (defaults to current directory)
# AIRFLOW_PROJ_DIR=.

# Optional: Additional pip requirements (for quick testing only)
# For production, extend the Airflow image instead
# _PIP_ADDITIONAL_REQUIREMENTS=
